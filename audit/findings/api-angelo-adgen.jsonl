{"partition":"api-angelo-adgen","category":"context","severity":"P3","file":"src/app/api/generate-video/route.ts","line":1,"evidence":"requireManageOrAdmin + requireFacilityAccess + applyRateLimit(RATE_LIMIT_TIERS) present; proxy-video has isAllowedUrl SSRF allowlist","claim_violated":"none (Angelo's domain — advisory only)","expected":"CONTEXT / ADVISORY (owner_domain:angelo, do not file as blocking): AI generation + ad/social publish routes (generate-copy/image/video/social-content/social-post, publish-ad/social, social-posts, stock-images, proxy-video, style-references) were scanned at the auth/abuse layer only. All carry auth gates (requireAdminKey / requireManageOrAdmin+requireFacilityAccess / isAdminRequest) and rate limiting; proxy-video implements a proper SSRF allowlist (blocks localhost/RFC1918/169.254.169.254/.internal/.local + host allowlist). No unauthenticated expensive-AI endpoint and no obvious cost-abuse or SSRF gap found in scope.","done_when":"n/a — advisory; deeper functional/cost review is Angelo's to own","completion_hint":"if a full adgen audit is wanted, assign to Angelo; proxy-video's isAllowedUrl is the SSRF pattern the v1 webhook path (SA finding) should reuse","owner_domain":"angelo","confidence":"medium"}
