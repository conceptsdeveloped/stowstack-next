{"partition":"contract","category":"contract-break","severity":"P1","file":"src/app/walkin/[code]/page.tsx","line":40,"evidence":"const res = await fetch(`/api/walkin-attribution?code=${encodeURIComponent(code)}`); ... if (!res.ok) { setInvalidCode(true); return; }","golden_path_step":9,"claim_violated":"walk-in attribution capture page validates a code and lets a manager log an in-person move-in","expected":"the validation GET must hit a handler that returns the facility for a valid code; /api/walkin-attribution exports only OPTIONS+POST (route.ts:10), so this GET returns 405 for every code, setting invalidCode=true and rendering the 'Invalid Access Code' screen (page.tsx:113) instead of the form — the entire walk-in page is non-functional for all users","done_when":"loading /walkin/<valid-code> renders the attribution form (not the invalid-code screen); a regression test asserts GET /api/walkin-attribution?code=<valid> returns 200 with facilityName, and the page shows the form","completion_hint":"add an `export async function GET` to src/app/api/walkin-attribution/route.ts that looks up the facility by access code and returns { facilityName } (scoped to that client), OR point the client validation at an existing endpoint that resolves a code to a facility","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"marker","severity":"P3","file":"src/app/help/page.tsx","line":107,"evidence":"{ icon: MessageCircle, label: \"Chat\", value: \"Coming soon\", href: undefined, external: false }","claim_violated":"help-page contact options are actionable","expected":"the 'Chat' contact card is a visible non-functional placeholder (href undefined, value 'Coming soon'). Intentional-deferred but user-visible on a public page.","done_when":"either wire the chat option to a real channel, or hide the card until chat ships, so the public help page shows no dead contact methods","completion_hint":"remove the Chat entry from the contact list (or gate it behind a flag) until live chat is implemented","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"context","severity":"P3","file":"src/app/_CONTRACT_VERDICT","line":0,"evidence":"Contract verdict: 1 P1 break, 1 P3 placeholder. Diffed every literal fetch('/api/...') call in src/app + src/components against the 203 route.ts files: ALL referenced static AND dynamic-segment paths resolve to an existing route (0 missing-route breaks; query-param design means no orphaned path segments). No variable-first-segment fetches (/api/${...}). Dead-UI scan clean: 0 href='#', 0 empty onClick/onSubmit handlers, 0 onClick={undefined}. The only verb-vs-route mismatch across all fetch sites is the walk-in GET above. alert() usages (admin pipeline/settings/campaign-wizard/social) are genuine error surfaces, not placeholders.","expected":"informational verdict for merge/COVERAGE","done_when":"n/a","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"contract-break","severity":"P1","file":"src/app/walkin/[code]/page.tsx","line":67,"evidence":"fetch(\"/api/walkin-attribution\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: ... }) — no x-admin-key/Bearer/x-org-token/x-csrf-token; /api/walkin-attribution is NOT in proxy.ts isCsrfExempt()","golden_path_step":9,"claim_violated":"a facility manager can log an in-person walk-in move-in","expected":"the walk-in submit POST passes the proxy CSRF gate (src/proxy.ts:138-147). It currently sends no exempting header and the path is not in isCsrfExempt(), so validateCsrf() fails and the proxy returns 403 before the route runs — the form submit is broken in prod (in addition to the GET-405 validation break already filed)","done_when":"submitting the walk-in form persists a walkin_attribution row in prod; a test asserts POST /api/walkin-attribution without a CSRF token is not rejected by the proxy (route added to isCsrfExempt, mirroring the other credential-in-body public routes)","completion_hint":"add `if (path === \"/api/walkin-attribution\") return true;` to isCsrfExempt() in src/proxy.ts (it authenticates via accessCode in the body, like resend-access-code/client-data)","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"contract-break","severity":"P1","file":"src/components/partner/partner-shell.tsx","line":74,"evidence":"fetch(\"/api/password-reset\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: { action: \"verify\"|\"request\", ... } }) — no exempting header; /api/password-reset not in isCsrfExempt()","golden_path_step":3,"claim_violated":"a partner/org user can reset their password","expected":"password-reset POST (both request and verify actions) is pre-authentication (no session, no admin key, no Bearer), so it carries no exempting header and the path is not in isCsrfExempt() -> proxy returns 403 in prod -> partners can never request or complete a password reset","done_when":"POST /api/password-reset succeeds in prod for both actions; /api/password-reset added to isCsrfExempt() and a test asserts the proxy does not 403 it","completion_hint":"add `if (path === \"/api/password-reset\") return true;` to isCsrfExempt() in src/proxy.ts (public pre-auth, rate-limited at the route)","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"contract-break","severity":"P1","file":"src/app/verify-email/page.tsx","line":23,"evidence":"fetch(\"/api/verify-email\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: { action: \"verify\", token } }) — no exempting header; /api/verify-email not in isCsrfExempt()","golden_path_step":3,"claim_violated":"a user can verify their email from the emailed link","expected":"the verify action runs from the emailed link with no ambient session yet, sends no exempting header, and the path is not in isCsrfExempt() -> proxy 403s it in prod -> email verification is impossible","done_when":"clicking the verification link verifies the email in prod; /api/verify-email added to isCsrfExempt() and a test asserts no proxy 403","completion_hint":"add `if (path === \"/api/verify-email\") return true;` to isCsrfExempt() in src/proxy.ts (token-in-body auth, rate-limited)","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"contract-break","severity":"P2","file":"src/app/manage/page.tsx","line":28,"evidence":"POST /api/manage/unlock and POST /api/manage/scratch (manage/page.tsx:52) send only Content-Type; /api/manage/* is NOT in isCsrfExempt(); additionally authHeaders() (src/lib/facility-auth.ts:47) sends x-manage-token, which proxy.ts does NOT treat as a CSRF-exempt header (only x-admin-key/Bearer/x-org-token)","claim_violated":"a facility owner can unlock a manage session and operate facility tabs without an admin key","expected":"the entire /manage owner-access flow is dead in prod: manage/unlock (login) and manage/scratch (create sandbox facility) 403 at the proxy CSRF gate, and even if a manage session existed, every facility-tab mutation made via authHeaders()'s x-manage-token would also 403 since x-manage-token isn't an exempt header. Admin-key users are unaffected, so this only blocks the non-admin owner path.","done_when":"manage/unlock + manage/scratch succeed in prod and a manage-session user can perform a facility-tab mutation; /api/manage/* added to isCsrfExempt() AND x-manage-token added to the exempt-header checks in isCsrfExempt()","completion_hint":"in src/proxy.ts isCsrfExempt(): add `if (path.startsWith(\"/api/manage/\")) return true;` and `if (req.headers.get(\"x-manage-token\")) return true;`","owner_domain":"blake","confidence":"high"}
{"partition":"contract","category":"contract-break","severity":"P2","file":"src/app/lp/[slug]/page.tsx","line":884,"evidence":"public landing page POSTs /api/meta-capi (line 884) and /api/page-interactions (line 922), and src/components/storedge/storedge-embed.tsx:142 POSTs /api/tracking/event — all with only Content-Type, none in isCsrfExempt()","golden_path_step":9,"claim_violated":"campaign landing pages emit conversion/interaction tracking used for attribution","expected":"these public, no-session tracking POSTs are not exempt and carry no token -> proxy 403s every one in prod. meta-capi (server-side conversion), page-interactions (engagement), and tracking/event (storEDGE embed funnel events) all silently fail (fire-and-forget .catch), so attribution loses LP-side signal","done_when":"LP tracking POSTs return 2xx in prod; /api/meta-capi, /api/page-interactions, /api/tracking/event (or /api/tracking/) added to isCsrfExempt() and a test asserts they are not 403'd","completion_hint":"add these public tracking paths to isCsrfExempt() in src/proxy.ts (they are unauthenticated, abuse-bounded by rate limits). meta-capi is Angelo's domain — coordinate.","owner_domain":"blake","confidence":"high"}
