{"partition":"api-billing-stripe","category":"contract-break","severity":"P2","file":"src/app/api/create-checkout-session/route.ts","line":11,"evidence":"export async function POST(req: NextRequest) { ... }  // no x-csrf-token check, not in proxy.ts isCsrfExempt() list; pricing page (src/app/pricing/page.tsx) is explicitly 'not a self-serve' flow and links to /#cta, never calls this route","golden_path_step":10,"claim_violated":"an operator can subscribe (agency revenue) end-to-end","expected":"either a self-serve checkout UI calls create-checkout-session with a valid CSRF token / via an exempted path, or checkout is documented as founder-initiated for pre-launch.","done_when":"if/when a browser CTA is wired to create-checkout-session, the route is added to proxy.ts isCsrfExempt() (it sends no x-admin-key/Bearer/x-org-token and no client sends x-csrf-token, so the proxy 403s it) OR the client sends the double-submit token; otherwise the self-serve checkout path is documented as intentionally manual.","completion_hint":"Same CSRF footgun that broke /portal login in prod: a mutating /api POST not in isCsrfExempt and not header-authed is 403'd before reaching the handler. create-billing-portal is exempt (proxy.ts:125) but create-checkout-session is not. Currently latent because no UI calls it.","owner_domain":"blake","confidence":"high"}
{"partition":"api-billing-stripe","category":"missing-impl","severity":"P1","file":"src/app/api/stripe-webhook/route.ts","line":88,"evidence":"const inviteToken = randomBytes(32).toString(\"hex\"); ... await tx.org_users.create({ data: { ... invite_token: inviteToken, password_hash: \"\" } });","golden_path_step":10,"claim_violated":"a paying operator can sign in after checkout completes","expected":"after checkout.session.completed creates the org + admin user, the operator receives an activation/welcome email with a tokenized set-password link (consuming invite_token) so they can log into /partner.","done_when":"checkout.session.completed dispatches an activation email keyed on invite_token; a route consumes invite_token to set the initial password and grant first login; covered by a test asserting the email is sent and the token is consumable.","completion_hint":"invite_token is generated but never emailed and never consumed by any route; no welcome email is sent in handleCheckoutComplete. Not alpha-blocking (alpha = Blake's own portfolio, no self-serve checkout wired) but blocks the first real paying customer.","owner_domain":"blake","confidence":"high"}
{"partition":"api-billing-stripe","category":"missing-impl","severity":"P1","file":"src/app/api/checkout-success/route.ts","line":59,"evidence":"if (meta.userId) { await db.$executeRaw`UPDATE org_users SET setup_token_hash = ${setupTokenHash} ...` } ... return jsonResponse({ ..., tempPassword: meta.tempPassword, ... })","golden_path_step":10,"claim_violated":"post-checkout the operator is handed a working path into their account","expected":"checkout-success returns a usable activation credential backed by values that are actually set upstream.","done_when":"the dead setup_token_hash branch (meta.userId is never set) and the always-empty tempPassword are removed and replaced with the real invite_token activation path; a test proves the returned payload yields a working first login.","completion_hint":"meta.userId and meta.tempPassword are never set anywhere (neither the webhook nor create-checkout-session set them), so the setup_token_hash UPDATE never runs and tempPassword is always empty. Wire to the invite_token activation flow.","owner_domain":"blake","confidence":"high"}
{"partition":"api-billing-stripe","category":"schema-drift","severity":"P1","file":"src/app/api/client-invoices/route.ts","line":323,"evidence":"const invoices = await db.activity_log.findMany({ where: { type: \"invoice_sent\", ...(facilityId ? { facility_id: facilityId } : {}) }, ... }); return jsonResponse({ success: true, data: invoices });","claim_violated":"a single canonical invoice system of record (client_invoices) per the M5 migration","expected":"GET /api/client-invoices reads the canonical client_invoices table (as its own POST writes and as /api/client-billing GET reads), returning the documented invoice wire shape.","done_when":"client-invoices GET reads client_invoices (not activity_log) and returns the same invoice shape as client-billing, or this stale endpoint is removed; verified by a test asserting the source table.","completion_hint":"POST persists to client_invoices (line 254) but GET still lists activity_log rows of type 'invoice_sent' — two divergent invoice readers (client-billing reads client_invoices). Migrate or delete.","owner_domain":"blake","confidence":"high"}
{"partition":"api-billing-stripe","category":"config-gap","severity":"P2","file":"src/app/api/client-invoices/route.ts","line":15,"evidence":"const PLAN_PRICES: Record<string, number> = { launch: 499, growth: 999, portfolio: 1499 };  // vs src/lib/stripe.ts PLANS: launch 750, growth 1500, portfolio 0","claim_violated":"invoice amounts reflect the operator's actual subscription price","expected":"a single source of truth for plan pricing used by invoicing, the pricing page, and Stripe price IDs.","done_when":"plan prices come from one shared constant (or the canonical pricing page values) so invoiced management fees match subscription pricing; mismatch resolved and covered by a test.","completion_hint":"client-invoices hardcodes 499/999/1499 as the plan management fee while lib/stripe.ts PLANS hardcodes 750/1500/0. Consolidate; reconcile against src/app/pricing/page.tsx (canonical per CLAUDE.md).","owner_domain":"blake","confidence":"medium"}
{"partition":"api-billing-stripe","category":"integration-gap","severity":"P2","file":"src/app/api/stripe-webhook/route.ts","line":30,"evidence":"switch (event.type) { case \"checkout.session.completed\": ... case \"customer.subscription.updated\": ... case \"customer.subscription.deleted\": ... case \"invoice.payment_failed\": ... case \"invoice.payment_succeeded\": ... }","claim_violated":"Stripe handler covers the required subscription lifecycle events","expected":"customer.subscription.created handled, or an explicit comment documenting why checkout.session.completed + .updated suffice.","done_when":"either a customer.subscription.created case exists, or a code comment documents that subscription creation is fully covered by checkout.session.completed + customer.subscription.updated backfill.","completion_hint":"README_SWARM step 10 + PREALPHA methodology list customer.subscription.created as required coverage. In practice .updated backfills the subscription id, but the gap is undocumented.","owner_domain":"blake","confidence":"medium"}
{"partition":"api-billing-stripe","category":"integration-gap","severity":"P2","file":"src/app/api/stripe-webhook/route.ts","line":29,"evidence":"try { switch (event.type) { ... } } catch (err) { ... }  // no record of processed event.id; no idempotency-key store","claim_violated":"webhook is idempotent against Stripe at-least-once redelivery","expected":"processed Stripe event ids are recorded/deduped so a redelivered event cannot double-apply a non-idempotent effect.","done_when":"the handler records event.id and short-circuits on replay, or each handler is provably idempotent and that is documented/tested.","completion_hint":"Org creation is guarded by stripe_customer_id and the updateMany handlers are naturally idempotent, but there is no explicit event-id dedup.","owner_domain":"blake","confidence":"medium"}
{"partition":"api-billing-stripe","category":"error-handling","severity":"P2","file":"src/app/api/stripe-webhook/route.ts","line":75,"evidence":"if (!email || !plan || !companyName) return;","claim_violated":"a completed checkout always provisions an org or surfaces a failure","expected":"a checkout.session.completed with missing metadata is logged/alerted rather than silently dropped, so a paid customer is never left without an org.","done_when":"the early return logs an error and/or captures to Sentry (captureRouteError) with the session id; verified by a test that a metadata-less session triggers an alert.","completion_hint":"Our own create-checkout-session always sets metadata, but a Stripe-dashboard or Payment-Link checkout would hit this silent return — paid, no org, no signal. Relevant since checkout is currently founder-initiated.","owner_domain":"blake","confidence":"medium"}
{"partition":"api-billing-stripe","category":"config-gap","severity":"P3","file":"src/app/api/create-checkout-session/route.ts","line":25,"evidence":"const validPlans = [\"launch\", \"growth\", \"portfolio\", \"enterprise\"]; ... const priceMap = { launch:..., growth:..., portfolio:... };","claim_violated":"plan set matches the productized tiers (no Enterprise tier per CLAUDE.md)","expected":"validPlans matches the real tiers; no dead 'enterprise' branch.","done_when":"'enterprise' is removed from validPlans (it passes validation then fails at priceId lookup with a misleading 'Invalid plan'), aligning with CLAUDE.md 'No Enterprise tier'.","completion_hint":"Drop 'enterprise' from validPlans or add a real price; currently a misleading dead branch.","owner_domain":"blake","confidence":"high"}
{"partition":"api-billing-stripe","category":"context","severity":"P3","file":"src/app/pricing/page.tsx","line":1,"evidence":"Pricing page copy: 'This isn't a self-serve [signup]. StorageAds is built for operators...' CTAs link to / and /#cta; no client code calls /api/create-checkout-session anywhere in src/.","golden_path_step":10,"claim_violated":"self-serve subscription checkout is wired","expected":"DEFERRED/founder-led — the full create-checkout-session -> checkout-success -> webhook provisioning chain is built but intentionally not exposed via UI pre-launch (alpha = Blake's own portfolio).","done_when":"N/A — context note. Confirm self-serve checkout being unwired is intentional for alpha; the activation-email + CSRF-exempt gaps above must be closed before wiring a self-serve CTA.","completion_hint":"Not a defect for alpha; recorded so the build swarm wires activation email + CSRF exemption together when self-serve billing is turned on.","owner_domain":"blake","confidence":"high"}
