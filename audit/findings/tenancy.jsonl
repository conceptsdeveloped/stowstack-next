{"partition":"tenancy","category":"tenancy-breach","severity":"P1","file":"src/app/api/tenants/route.ts","line":64,"evidence":"if (tenantId) { ... WHERE t.id = ${tenantId}::uuid ... } (all sub-queries scoped only by tenant_id, never re-checked against the facility the caller is authorized for)","golden_path_step":8,"claim_violated":"tenant/renter data is scoped to the operator's own facility","expected":"the ?tenantId branch must verify the resolved tenant.facility_id is one the caller is authorized for (admin, or manageScopeAllows(scope, tenant.facility_id)), not merely that the caller passed an owned ?facilityId","done_when":"a manage-session scoped to facility A that requests ?facilityId=A&tenantId=<tenant of facility B> receives 401/404, and a regression test asserts cross-facility tenantId reads are denied","completion_hint":"after fetching the tenant by id, resolve its facility_id and call requireFacilityAccess(req, tenant.facility_id) (mirror the pattern already used in call-logs PATCH / facility-context DELETE) before returning payments/escalations/churn/communications","owner_domain":"blake","confidence":"high"}
{"partition":"tenancy","category":"context","severity":"P3","file":"src/app/api/tenants/route.ts","line":54,"evidence":"requireFacilityAccess(req) with no explicit facilityId falls back to the ?facilityId query param; the helper validates that param but cannot know the actual record being queried in id/tenantId branches","expected":"NOTE (not a standalone fix): the requireFacilityAccess(req)-with-fallback pattern is safe only when the handler queries by the SAME ?facilityId it validated. Any branch that pivots to a different record id (tenantId, funnel id, etc.) must re-validate that record's facility. funnels GET ?id= is safe because a manage-session with no ?facilityId fails the guard (admin-only); tenants ?tenantId is NOT safe because the caller supplies an owned ?facilityId alongside a foreign tenantId.","done_when":"audit note only — see SA tenants IDOR finding above","owner_domain":"blake","confidence":"high"}
