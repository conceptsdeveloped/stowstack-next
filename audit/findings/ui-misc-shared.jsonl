{"partition":"ui-misc-shared","category":"scope-gap","severity":"P3","file":"src/app/error.tsx","line":26,"evidence":"className=\"... bg-[var(--color-gold)] ... hover:bg-[var(--color-gold-hover)]\" on the 'Try again' button of the root error boundary","claim_violated":"design system bans gold everywhere except the logo 'ads' lockup (CLAUDE.md / design-system.md)","expected":"The root error boundary CTA uses the sanctioned charcoal-on-light / light-on-dark CTA tokens, not the banned legacy --color-gold* tokens.","done_when":"src/app/error.tsx 'Try again' button uses --color-dark/--color-light (or an approved CTA token) instead of --color-gold/--color-gold-hover.","completion_hint":"swap gold tokens for the standard primary CTA classes used elsewhere; user-visible production surface.","owner_domain":"blake","confidence":"high"}
{"partition":"ui-misc-shared","category":"scope-gap","severity":"P3","file":"src/components/theme-provider.tsx","line":1,"evidence":"ThemeProvider/ThemeToggle are imported only by src/components/__tests__/theme-toggle.test.tsx and referenced in a globals.css comment; no app/layout.tsx or page mounts them","claim_violated":"a shipped theme toggle is wired into the app","expected":"Either the theme provider/toggle is mounted (rendered in a layout/shell) or removed as dead code; tests should not be the only consumer.","done_when":"ThemeProvider/ThemeToggle are either mounted in the real component tree or deleted; no exported-but-never-rendered theme UI remains.","completion_hint":"confirm intent (theme switching is not part of the light-only design system) and remove, or mount if intended.","owner_domain":"blake","confidence":"high"}
