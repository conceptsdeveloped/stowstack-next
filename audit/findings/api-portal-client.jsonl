{"partition":"api-portal-client","category":"config-gap","severity":"P1","file":"src/app/api/client-messages/route.ts","line":84,"evidence":"const rows = await db.client_messages.findMany({ where: { client_id: clientId }, ... })","golden_path_step":3,"claim_violated":"the finished portal two-way messaging UI is backed by a durable Postgres store","expected":"the client_messages table exists in prod so the portal messages thread loads and sends","done_when":"prisma/manual/2026-06-28-customer-dashboard-tables.sql is applied to prod (npx prisma db execute) and verified; GET/POST /api/client-messages return 200 against the real table instead of throwing (caught -> 500). There is NO migration that creates this table (prisma/migrations has only 0_init); the model exists in schema.prisma so typecheck/build pass and the gap is invisible until runtime.","completion_hint":"apply the documented manual DDL to prod, then redeploy so the generated client matches; consider promoting it to a real migration so deploys are self-applying","owner_domain":"blake","confidence":"medium"}
{"partition":"api-portal-client","category":"config-gap","severity":"P1","file":"src/app/api/client-billing/route.ts","line":110,"evidence":"const rows = await db.client_invoices.findMany({ orderBy: { issued_at: \"desc\" }, ... })","golden_path_step":10,"claim_violated":"M5: invoices are a single Postgres system of record surfaced in the portal billing tab","expected":"the client_invoices table exists in prod so portal billing (GET) and admin invoice authoring (POST/PATCH) work","done_when":"prisma/manual/2026-06-28-customer-dashboard-tables.sql applied to prod and verified; GET/POST/PATCH /api/client-billing operate on the real client_invoices table instead of throwing (caught -> 500). Same un-migrated-table gap as client_messages: model is in schema.prisma but no migration creates it.","completion_hint":"apply the documented manual DDL; promote to a migration to make it deploy-safe","owner_domain":"blake","confidence":"medium"}
{"partition":"api-portal-client","category":"contract-break","severity":"P2","file":"src/app/api/client-invoices/route.ts","line":323,"evidence":"const invoices = await db.activity_log.findMany({ where: { type: \"invoice_sent\", ... } })","golden_path_step":10,"claim_violated":"client_invoices is the single system of record for invoices (M5)","expected":"both portal invoice surfaces read the same canonical store","done_when":"client-invoices GET reads the canonical client_invoices table (like client-billing GET) rather than the legacy activity_log 'invoice_sent' events, OR this GET is removed in favor of client-billing GET; the two portal invoice views can no longer disagree (POST writes client_invoices + activity_log; this GET reads only activity_log).","completion_hint":"point this GET at db.client_invoices scoped by client/facility, or delete it and consolidate on client-billing","owner_domain":"blake","confidence":"high"}
{"partition":"api-portal-client","category":"scope-gap","severity":"P3","file":"src/app/api/portal-upload/route.ts","line":204,"evidence":"PDF/XLSX stay \"uploaded\" for manual handling (parser is CSV-only by design) — only text/csv is auto-processed (line 208).","golden_path_step":5,"claim_violated":"uploaded PMS reports are ingested","expected":"non-CSV uploads have a real manual-processing path, not a dead-end","done_when":"confirm an admin workflow exists to process PDF/Excel uploads that sit at status \"uploaded\" (cross-ref Scout 5 api-pms-intel/ui-admin admin-pms-queue). The portal UI already correctly discloses this (upload/page.tsx:244 \"CSV process automatically. PDF and Excel are reviewed by our team\"), so this is NOT a user-facing contract break — the only residual risk is non-CSV uploads accumulating with no admin action surface. The API 415 message advertising CSV/PDF/Excel is accurate.","completion_hint":"verify admin-pms-queue surfaces status=uploaded non-CSV files for manual processing; if not, add it","owner_domain":"blake","confidence":"medium"}
