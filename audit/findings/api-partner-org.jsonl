{"partition":"api-partner-org","category":"validation","severity":"P2","file":"src/app/api/org-email/route.ts","line":134,"evidence":"${vars.leadName ? `<p ...><strong>Name:</strong> ${vars.leadName}</p>` : \"\"}","claim_violated":"branded org emails render untrusted lead data safely","expected":"User-derived template variables (leadName, leadEmail, leadPhone, unitSize, source, facilityName, name) originate from public, unauthenticated lead-capture and are interpolated raw into the email HTML. They must be HTML-escaped before interpolation.","done_when":"all interpolated `vars.*` values in the welcome/campaign_live/monthly_report/lead_notification templates are passed through escapeHtml() (already used in partner-signup/org-users) so a lead name like `<img src=x onerror=...>` cannot inject markup into the operator-facing email","completion_hint":"reuse escapeHtml from src/lib/validation; wrap every ${vars.X} that is text content","owner_domain":"blake","confidence":"medium"}
{"partition":"api-partner-org","category":"validation","severity":"P3","file":"src/app/api/referrals/route.ts","line":283,"evidence":"const { code_id, amount, description } = body; ... if (parseFloat(String(codeRow.credit_balance)) < amount) ... const newBalance = parseFloat(String(codeRow.credit_balance)) - amount;","claim_violated":"referral credit redemption cannot corrupt a balance","expected":"`amount` is used in arithmetic and a balance check without validating it is a positive, finite number. A negative or non-numeric `amount` bypasses the insufficient-balance guard and inflates credit_balance (newBalance = balance - (negative)).","done_when":"redeem rejects amount unless it is a finite number > 0 (and <= credit_balance); same numeric guard applied wherever amount feeds arithmetic","completion_hint":"validate `typeof amount === 'number' && Number.isFinite(amount) && amount > 0` before the balance check; admin-key gated so impact is low","owner_domain":"blake","confidence":"high"}
{"partition":"api-partner-org","category":"validation","severity":"P3","file":"src/app/api/org-activity/route.ts","line":77,"evidence":"const activity = await db.activity_log.create({ data: { type, facility_id: facilityId || null, facility_name: facilityName || null, detail } });","claim_violated":"activity rows are bound to the writer's org","expected":"An authenticated org user can create an activity_log row with arbitrary type/facility_name/detail; when facilityId is omitted the row is written with facility_id null and no organization binding. GET only returns rows JOINed to facilities in the caller's org, so these become unreadable orphan rows (no cross-tenant leak), but the write is unvalidated/unscoped.","done_when":"org-activity POST requires a facilityId that belongs to the caller's org (reject when absent) so every activity row is org-bound, OR drop the write path if unused","completion_hint":"make facility ownership check mandatory (currently only runs `if (facilityId && orgId)`); low severity since GET is org-scoped","owner_domain":"blake","confidence":"medium"}
{"partition":"api-partner-org","category":"scope-gap","severity":"P3","file":"src/app/api/organizations/route.ts","line":183,"evidence":"const validPlans = [\"starter\", \"growth\", \"enterprise\"];","claim_violated":"plan tiers match the canonical product pricing","expected":"Org/partner signup accepts plan `enterprise`, but CLAUDE.md states there is no Enterprise tier and the product tiers are Launch/Growth/Portfolio (subscriptions). The org-plan vocabulary (starter/growth/enterprise) diverges from product pricing; same list duplicated in partner-signup/route.ts:66. Ambiguous whether org-plan == product-tier (filed as scope-gap per audit S2).","done_when":"org plan vocabulary is reconciled with the canonical pricing source (src/app/pricing/page.tsx) or explicitly documented as a distinct partner-tier scale, and the two duplicate validPlans arrays share one source of truth","completion_hint":"confirm with Blake whether partner org tiers are intentionally named differently; if not, align names and centralize the list","owner_domain":"blake","confidence":"low"}
