{"partition":"api-attribution-tracking","category":"integration-gap","severity":"P0","file":"src/app/api/walkin-attribution/route.ts","line":10,"evidence":"export async function POST(req: NextRequest) — route is NOT listed in isCsrfExempt() (src/proxy.ts:80-130); caller src/app/walkin/[code]/page.tsx:67 POSTs with headers { 'Content-Type': 'application/json' } only (no x-csrf-token, no x-admin-key/Bearer/x-org-token).","golden_path_step":9,"claim_violated":"walk-in move-ins are attributed back to ad source","expected":"A facility manager on /walkin/[code] can submit a walk-in attribution and it is recorded.","done_when":"POST /api/walkin-attribution succeeds (200, row persisted) when called from the public /walkin/[code] page in production; either the path is added to isCsrfExempt() in src/proxy.ts (it authenticates via accessCode in the body, same model as the existing audit-form/consumer-lead/client-data exemptions) OR the client sends a valid double-submit x-csrf-token. Verified by a non-403 POST in prod.","completion_hint":"Add `if (path === \"/api/walkin-attribution\") return true;` to isCsrfExempt() in src/proxy.ts — it is body/accessCode authenticated and rate-limited, matching the existing public-lead exemptions. proxy.ts requiresCsrf('POST')=true + not exempt + no token = hard 403 'Invalid or missing CSRF token'.","owner_domain":"blake","confidence":"high"}
{"partition":"api-attribution-tracking","category":"integration-gap","severity":"P1","file":"src/app/api/tracking/visit/route.ts","line":17,"evidence":"POST not in isCsrfExempt() (src/proxy.ts); caller src/hooks/use-tracking-params.ts:48 fires fetch('/api/tracking/visit',{method:'POST',headers:{'Content-Type':'application/json'}}).catch(()=>{}) — no x-csrf-token, failure swallowed silently.","golden_path_step":9,"claim_violated":"paid-click visits (gclid/fbclid/utm) are captured for attribution","expected":"Visiting any page with paid UTM params records a tracking_visit row tying the session to the campaign.","done_when":"POST /api/tracking/visit returns 200 and persists a visit in prod; add the path to isCsrfExempt() in src/proxy.ts OR have the hook send a valid x-csrf-token. Verified by a non-403 POST in prod.","completion_hint":"Add `/api/tracking/visit` to isCsrfExempt() (public anonymous beacon, no session, rate-limited PUBLIC_WRITE). Currently 403s in prod and the .catch(()=>{}) hides it, so visit/UTM attribution silently records nothing.","owner_domain":"blake","confidence":"high"}
{"partition":"api-attribution-tracking","category":"integration-gap","severity":"P1","file":"src/app/api/tracking/event/route.ts","line":17,"evidence":"POST not in isCsrfExempt() (src/proxy.ts); caller src/components/storedge/storedge-embed.tsx:142 fires fetch('/api/tracking/event',{method:'POST',headers:{'Content-Type':'application/json'}}).catch(()=>{}) — no x-csrf-token, failure swallowed silently.","golden_path_step":9,"claim_violated":"reservation/move-in funnel events from the storEDGE embed are captured","expected":"storEDGE embed funnel events (view/step/reserve) POST to /api/tracking/event and are recorded against the facility/landing page.","done_when":"POST /api/tracking/event returns 200 and persists in prod; add the path to isCsrfExempt() in src/proxy.ts OR send a valid x-csrf-token. Verified by a non-403 POST in prod.","completion_hint":"Add `/api/tracking/event` to isCsrfExempt() (public anonymous embed beacon, rate-limited). Currently 403s in prod; the embed's .catch(()=>{}) hides the failure, so storEDGE funnel events are silently dropped.","owner_domain":"blake","confidence":"high"}
{"partition":"api-attribution-tracking","category":"integration-gap","severity":"P1","file":"src/app/api/page-interactions/route.ts","line":32,"evidence":"POST not in isCsrfExempt() (src/proxy.ts); caller src/app/lp/[slug]/page.tsx:922 fires fetch('/api/page-interactions',{method:'POST',headers:{'Content-Type':'application/json'},keepalive:true}).catch(()=>{}) — no x-csrf-token, failure swallowed silently.","golden_path_step":9,"claim_violated":"landing-page engagement (scroll depth, time-on-page, clicks) is captured for A/B and attribution","expected":"Landing-page interaction heartbeats POST to /api/page-interactions and persist heatmap/engagement rows.","done_when":"POST /api/page-interactions returns 200 and persists in prod; add the path to isCsrfExempt() in src/proxy.ts OR send a valid x-csrf-token. Verified by a non-403 POST in prod.","completion_hint":"Add `/api/page-interactions` to isCsrfExempt() (public anonymous landing-page beacon, IP rate-limited 30/60s). Currently 403s in prod; .catch(()=>{}) hides it, so landing-page analytics record nothing.","owner_domain":"blake","confidence":"high"}
{"partition":"api-attribution-tracking","category":"integration-gap","severity":"P1","file":"src/app/api/meta-capi/route.ts","line":221,"evidence":"POST not in isCsrfExempt() (src/proxy.ts); caller src/app/lp/[slug]/page.tsx:884 fires fetch('/api/meta-capi',{method:'POST',...}) from the browser with no x-csrf-token. NOTE: meta-capi route logic is Angelo's domain; the fix is in proxy.ts (Blake's).","golden_path_step":9,"claim_violated":"server-side Meta conversion events (CAPI) fire for ad attribution","expected":"Browser-side landing page triggers /api/meta-capi which forwards a server-side conversion event to Meta.","done_when":"POST /api/meta-capi from the browser returns non-403 in prod; add the path to isCsrfExempt() in src/proxy.ts OR send a valid x-csrf-token. Verified by a non-403 POST in prod.","completion_hint":"Add `/api/meta-capi` (and likely `/api/google-conversion`) to isCsrfExempt() in src/proxy.ts. Advisory: route internals are Angelo's; the CSRF exemption is Blake's proxy.ts. Currently the browser-fired CAPI call 403s in prod.","owner_domain":"angelo","confidence":"medium"}
{"partition":"api-attribution-tracking","category":"validation","severity":"P2","file":"src/app/api/page-interactions/route.ts","line":45,"evidence":"const { landingPageId, facilityId, ... } = body; if (!landingPageId || !facilityId) { return errorResponse('Missing required fields',400) } — no isValidUuid() check and no ownership verification, unlike sibling routes src/app/api/tracking/event/route.ts:32 and tracking/visit/route.ts:38 which call isValidUuid().","golden_path_step":9,"claim_violated":"per-facility analytics are accurate and isolated","expected":"page-interactions validates landingPageId/facilityId as UUIDs (and ideally confirms the landing page belongs to the facility) before inserting, matching the sibling tracking routes.","done_when":"page-interactions rejects or null-guards non-UUID landingPageId/facilityId (via isValidUuid from @/lib/validation) so a public caller cannot write arbitrary cross-facility analytics rows or trigger raw-SQL type errors; covered by a test.","completion_hint":"Import isValidUuid from @/lib/validation and validate landingPageId & facilityId (same pattern as tracking/event + tracking/visit). Public route accepts both IDs straight from the untrusted body into $executeRaw INSERTs.","owner_domain":"blake","confidence":"high"}
