{"partition":"lib-core","category":"stub-return","severity":"P3","file":"src/lib/v1-auth.ts","line":87,"evidence":"INSERT INTO api_usage_log (api_key_id, organization_id, method, path, status_code, duration_ms) VALUES (${row.id}::uuid, ${row.organization_id}::uuid, ${method}, ${path}, 200, ${duration})","expected":"api_usage_log should record the actual HTTP status of the request; logging happens inside requireApiAuth before the handler runs, so the real status (4xx/5xx) is never captured and every logged row reads 200","done_when":"api_usage_log rows reflect the true response status (e.g. log written from a route wrapper after the handler resolves) instead of a constant 200","completion_hint":"Move usage logging into a response wrapper that knows the final status, or backfill status via a deferred update keyed on api_key_id+path+timestamp","owner_domain":"blake","confidence":"high"}
{"partition":"lib-core","category":"validation","severity":"P3","file":"src/lib/validation.ts","line":0,"evidence":"escapeHtml does not escape single quotes (')","expected":"escapeHtml should escape ' to &#39; so output is safe in single-quoted or unquoted HTML attribute contexts, not just text/double-quoted contexts","done_when":"escapeHtml escapes & < > \" ' (and ideally /); a unit test asserts a single-quote payload is neutralized","completion_hint":"Add the ' -> &#39; replacement to the escape map in escapeHtml","owner_domain":"blake","confidence":"low"}
